# 🚀 KnightCycle Production Environment Variables
# Copy this to IONOS environment variables or use as .env in production

# === Core Django Settings ===
DEBUG=False
SECRET_KEY=GENERATE-NEW-SECRET-KEY-HERE-USE-PYTHON-SECRETS-MODULE
ALLOWED_HOSTS=knightcycle.co.uk,www.knightcycle.co.uk

# === Site Configuration ===
SITE_URL=https://knightcycle.co.uk

# === Database (if using PostgreSQL) ===
# Uncomment if migrating from SQLite to PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost:5432/knightcycle_db

# === Email Configuration (IONOS SMTP) ===
EMAIL_HOST=smtp.ionos.co.uk
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=noreply@knightcycle.co.uk
EMAIL_HOST_PASSWORD=your-email-password-here
DEFAULT_FROM_EMAIL=noreply@knightcycle.co.uk

# === MailerLite Integration ===
# Get your API key from: https://dashboard.mailerlite.com/integrations/api
MAILERLITE_API_KEY=your-mailerlite-api-key-here
MAILERLITE_GROUP_ID=  # Optional: Leave blank if not using specific groups

# === Notes ===
# 1. Generate SECRET_KEY with: python -c "import secrets; print(secrets.token_urlsafe(50))"
# 2. Never commit this file to version control
# 3. Set these as environment variables in IONOS hosting panel
# 4. Test locally with DEBUG=True first, then switch to False for production
